#pragma once
#include <iostream>
#include <fstream>
#include <sstream>
#include <algorithm>
using namespace std;

bool generate(string);
bool print(string);
bool append(string,string);
int search(string,unsigned int);
bool count(string);
bool merge(string);